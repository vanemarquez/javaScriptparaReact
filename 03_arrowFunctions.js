// Función normal para sumar dos números
function sumar(a, b) {
    return a + b;
  }
  
  console.log(sumar(3, 5)); 
  
// Misma función pero utilizando arrow function
  const sumarArrow = (a, b) => a + b;
  
  console.log(sumarArrow(3, 5));